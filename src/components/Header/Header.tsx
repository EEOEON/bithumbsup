import HeaderTop from './HeaderTop';
import HeaderNavBar from './HeaderNavBar';

const Header = () => {
  return (
    <header>
      <HeaderTop />
      <HeaderNavBar />
    </header>
  );
};

export default Header;
